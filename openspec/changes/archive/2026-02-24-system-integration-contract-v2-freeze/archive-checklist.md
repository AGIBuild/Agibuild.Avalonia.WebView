- [x] Proposal/design/specs/tasks generated and validated under OpenSpec strict mode.
- [x] Reserved metadata key registry and extension lane implemented with deterministic deny taxonomy.
- [x] `OccurredAtUtc` canonical UTC millisecond normalization implemented with CT/IT evidence.
- [x] Template ShowAbout runtime toggle marker implemented with default deny behavior preserved.
- [x] Governance and CT matrix assertions updated for registry/timestamp freeze branches.
- [x] `nuke Test` passed (Unit 764, Integration 148, Total 912).
- [x] `nuke Coverage` passed (Line coverage 95.96% >= 90% threshold).

## Open Questions

1. Should reserved keys be promoted to public host SDK constants for compile-time reuse?
2. Do we want to archive this change immediately or continue with main-spec sync plus next Phase 5 increment first?
