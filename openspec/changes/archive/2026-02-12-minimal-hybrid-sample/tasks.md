# minimal-hybrid-sample — Tasks

## Task 1: Create samples/minimal-hybrid/wwwroot/index.html
**Acceptance**: Single HTML file with inline JS; demonstrates `waitForBridge`, `invoke` for `getCurrentUser`, `saveSettings`, `searchItems`; modern glass-morphism UI.
