## 1. 模板内容补齐

- [x] 1.1 新增 React/Vue Web 模板目录与最小可构建骨架。
- [x] 1.2 更新 `template.json` 的条件排除逻辑，确保 framework 选择生效。

## 2. E2E 验证升级

- [x] 2.1 扩展 `TemplateE2E`，验证 react 路径 npm build。
- [x] 2.2 扩展 `TemplateE2E`，验证 vue 路径 npm build。

## 3. 规范与验证

- [x] 3.1 更新对应 OpenSpec delta spec。
- [x] 3.2 运行相关测试与 `openspec validate --all --strict`。
