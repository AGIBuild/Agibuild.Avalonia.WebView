## 1. Typed Bridge Client 收敛

- [x] 1.1 更新 `packages/bridge` 的 typed service contract 与参数约束。
- [x] 1.2 迁移 React 示例 bridge services 到 typed client。
- [x] 1.3 迁移 Vue 示例 bridge services 到 typed client。

## 2. DX 资产治理

- [x] 2.1 更新治理测试断言，校验 typed client 用法。

## 3. 验证

- [x] 3.1 运行相关 .NET 治理测试通过。
- [x] 3.2 运行 React/Vue web build（或 tsc）通过。
- [x] 3.3 运行 `openspec validate --all --strict` 通过。
