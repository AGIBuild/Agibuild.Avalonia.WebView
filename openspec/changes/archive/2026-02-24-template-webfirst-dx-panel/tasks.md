## 1. Template Strategy Panel (Deliverable 5.4)

- [x] 1.1 Add deterministic strategy visualization panel in template web demo for system integration outcomes.
- [x] 1.2 Include structured result line format (`mode`, `action`, `outcome`, `reason`) for machine parsing.

## 2. ShowAbout Scenario Switching (Deliverable 5.4)

- [x] 2.1 Add one-click deny/allow scenario switching path for ShowAbout in template flow.
- [x] 2.2 Keep secure-by-default path unchanged and explicitly test default deny branch.

## 3. Reusable Demo Regression Script (Deliverable 5.5)

- [x] 3.1 Expose reusable template regression check function callable by AI agents.
- [x] 3.2 Add governance and unit assertions for script marker and output contract stability.

## 4. Verification and Archive

- [x] 4.1 Run `openspec validate --all --strict` and relevant test suites (plus full `nuke Test` if needed).
- [x] 4.2 Record verification evidence and archive this change before starting the next task.
