# Deep Interview Context Snapshot

- Task statement: `...` (explicit deep-interview invocation with no concrete task yet)
- Desired outcome: Clarify what the user actually wants to change, decide scope boundaries, and produce an execution-ready spec instead of guessing.
- Stated solution: Start a deep-interview clarification loop before any planning or implementation.
- Probable intent hypothesis: The user wants an intent-first requirements interview for some Fulora-related change but intentionally withheld specifics to avoid premature assumptions.
- Known facts/evidence:
  - This is an established brownfield repository for Fulora, a typed hybrid app platform for .NET and Avalonia.
  - The workspace already contains substantial product surface under `src/`, `plugins/`, `templates/`, `samples/`, `tests/`, and `docs/`.
  - README positions Fulora around typed bridge contracts, SPA hosting, native capabilities, CLI flows, plugins, telemetry, and packaging.
  - `omx explore` was attempted for brownfield lookup but is unavailable in this environment because `cargo` is missing; fallback inspection was used instead.
- Constraints:
  - The task description is currently unspecified beyond `...`.
  - Deep-interview mode must clarify intent and boundaries before any implementation.
  - Only one interview question should be asked per round.
- Unknowns/open questions:
  - What problem or opportunity the user wants to address.
  - Whether the target is product strategy, docs, API design, implementation, DX, testing, packaging, ecosystem, or another area.
  - What outcome would count as success.
  - What is explicitly out of scope.
- Decision-boundary unknowns:
  - What OMX may decide without further confirmation.
  - How much freedom exists on architecture, prioritization, and output format.
- Likely codebase touchpoints:
  - `README.md`
  - `docs/`
  - `src/`
  - `plugins/`
  - `templates/`
  - `samples/`
  - `tests/`
