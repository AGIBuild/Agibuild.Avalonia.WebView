<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:agw="using:Agibuild.Fulora"
        x:Class="Agibuild.Fulora.Integration.NugetPackageTests.MainWindow"
        Title="NuGet Package Smoke Test" Width="800" Height="600">
  <DockPanel>
    <TextBlock x:Name="StatusText" DockPanel.Dock="Top" Margin="8"
               FontSize="14" Foreground="Gray"
               Text="Starting smoke test..." />
    <!-- WebView created via XAML — relies on UseAgibuildWebView() DI initialization -->
    <agw:WebView x:Name="WebViewControl" />
  </DockPanel>
</Window>
